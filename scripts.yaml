turn_off_all_light:
  alias: Turn off all light
  sequence:
  - service: light.turn_off
    target:
      entity_id:
      - light.bedroom
      - light.kitchen
      - light.dinning_room
      - light.living_room
  mode: single
